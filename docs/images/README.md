# README 截图目录（docs/images）

README 通过 GitHub raw 绝对地址引用本目录下的图片：

| 文件 | 内容 |
| --- | --- |
| `tab-overview.png` | 入口与内容预览：Network 选中 SSE 请求 + Response 区的 `SSE` 标签（红框）+ 右上角 `View All` / `Copy All`（红框） |
| `view-all.png` | `View All` 近全屏弹窗（标题栏含请求 URL、字符数、消息条数） |

按上面的文件名放进来即可（覆盖同名文件即更新），无需改 README。
本目录不参与 npm 发布（`package.json` 的 `files` 为白名单）。

截图建议：仅使用本地/演示请求（如 `127.0.0.1:39999`），避免出现真实域名、Cookie、Token。
